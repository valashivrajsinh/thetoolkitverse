
// This file is intentionally left blank or with minimal configuration.
//
// As per best practices and security guidelines, API keys and other secrets
// are not stored in the source code. They are accessed directly from
// environment variables in the relevant service files (e.g., `services/geminiService.ts`).
//
// Example:
// In `services/geminiService.ts`, the API key is retrieved using `process.env.API_KEY`.
//
// This ensures that sensitive credentials are kept separate from the codebase.

export {};